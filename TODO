Bugs:
    - Depth limit doesn't work with current implementation
    - Fix repeat runs capability
      - Stopping and starting a new run is unreliable and buggy

Working:
   Pass the user's QueryModel to the threads that request it


Generate a documentation page to show how to use the GUI
Add logging to application to learn why it's useful and how to do it

Possibly add a tab to view result...
 Would be some sort of table / tree view


May have to use 2 gson parsers
1 should not escape and 1 should.


reorder inputs to enable tab completion in order

enable load JSON from file

figure out better format to output json. the current format is very repetitive