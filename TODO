Bugs:
    - Depth limit doesn't work with current implementation

Working:
    Make UI responsive.
        -Right now expanding the window just shows whitespace...


    Create text extraction tab
    Two Panes
    Query JSON | Results JSON
               |
               |

    The results JSON pane is based on the queries from the given BASE URL in the HTML TAB
